<h1>
  Devour It!
</h1>
<div class="container">
  <ul>
    {{#each burgers}}
      {{#unless devoured}}
        <li>
          {{burger_name}}
          <button class="view" data-id="{{id}}" type="submit">
           EAT ME!
          </button>
        </li>
        {{!-- {{> burgers/burger-block devored=true}} --}}
      {{/unless}}
    {{/each}}
  </ul>
  <ul>
    {{#each burgers}}
      {{#if devoured}}
        <li>
          {{burger_name}}
          </button>
        </li>
      {{/if}}
    {{/each}}
  </ul>
</div>
<h2>
  Add Your Burger
</h2>
<form class="create-form">
  <div class="form-group">
    <input type="text" id="ca" name="name">
  </div>
  <button type="submit">
    Submit
  </button>
</form>